services:
  - type: web
    name: churn-radar
    env: python
    buildCommand: "pip install -r requirements.txt && python3 pipeline.py"
    startCommand: "streamlit run streamlit_app.py --server.port $PORT --server.address 0.0.0.0"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
